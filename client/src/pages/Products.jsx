import React from 'react'
import { Card, Typography } from 'antd'

const { Title } = Typography

export default function Products() {
  return (
    <div>
      <Title level={2}>Products</Title>
      <Card>
        <p>Product management page will be here.</p>
      </Card>
    </div>
  )
} 