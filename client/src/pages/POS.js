import React from 'react'
import { Card, Typography } from 'antd'

const { Title } = Typography

export default function POS() {
  return (
    <div>
      <Title level={2}>Point of Sale</Title>
      <Card>
        <p>POS system interface will be here.</p>
      </Card>
    </div>
  )
} 